{
  "name": "voxel-craft-fps",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "cap:build": "npm run build && npx cap sync ios",
    "cap:open": "npx cap open ios"
  },
  "dependencies": {
    "@capacitor/core": "^8.3.0",
    "@capacitor/ios": "^8.3.0",
    "@dimforge/rapier3d-compat": "^0.14.0",
    "simplex-noise": "^4.0.3",
    "three": "^0.170.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.3.0",
    "@types/three": "^0.170.0",
    "typescript": "^5.7.0",
    "vite": "^6.0.0"
  }
}
